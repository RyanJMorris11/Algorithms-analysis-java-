Should accept an input.txt absolute path via command prompt

Otherwise it will look for the file in the current directory

By default it will use the hard coded String abolsolutePathForInputFile 
	if the file isn't found